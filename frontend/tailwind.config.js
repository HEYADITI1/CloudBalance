import { colors } from "@mui/material";
import { plugin } from "postcss";

export default {
    theme: {
        extend: {
            colors:{
                primary: "#6366f1",
            }
        }
    },
    plugins: [],
};